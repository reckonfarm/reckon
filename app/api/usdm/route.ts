// Alias of /api/layers/usdm (the registry's USDM vector proxy). Kept so the hay map's
// fetch('/api/usdm') keeps working unchanged after the layer-registry refactor.
export { GET } from '@/app/api/layers/usdm/route'
